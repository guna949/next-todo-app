# Local development only. In production on Cloudflare Workers, the
# connection string instead comes from the HYPERDRIVE binding — see
# wrangler.jsonc and README.md.
DATABASE_URL="postgresql://user:password@localhost:5432/todoapp"
